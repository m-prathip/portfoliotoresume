services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: resume_platform
    ports:
      - "5432:5432"
    volumes:
      - resume_platform_db:/var/lib/postgresql/data

volumes:
  resume_platform_db:
